# OpenSpec Project Scaffold

This module provides reusable OpenSpec folders and templates for new repositories.

## Base folders

- `openspec/changes/`
- `openspec/specs/`

## Templates

- `openspec/templates/change/`

Copy `openspec/templates/change/` to a new `openspec/changes/<change-name>/` directory when starting a change.
